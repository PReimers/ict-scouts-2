<nav class="navbar navbar-inverse navbar-fixed-top">
    <div class="container">
        <div class="navbar-header">
            <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#navbar" aria-expanded="false" aria-controls="navbar">
                <span class="sr-only">Toggle navigation</span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
            </button>
            <a class="navbar-brand" href="{{ url('home') }}">ICT-Scouts / Campus</a>
        </div>
        <div id="navbar" class="collapse navbar-collapse">
            <ul class="nav navbar-nav">
                <li class="{{ nav_is_current(current_path, 'home') }}"><a href="{{ url('home') }}">Home</a></li>
                {% if app.user %}
                    {% include 'nav/user.html.twig' %}
                {% else %}
                    <li class="{{ nav_is_current(current_path, 'login') }}"><a href="{{ url('login') }}">Login</a></li>
                {% endif %}
                <li class="{{ nav_is_current(current_path, 'imprint') }}"><a href="{{ url('imprint') }}">Impressum</a></li>
            </ul>
        </div><!--/.nav-collapse -->
    </div>
</nav>
